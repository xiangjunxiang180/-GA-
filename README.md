# 遗传算法求解二元函数最大值

## 功能介绍
本代码实现了遗传算法，用于求解二元函数 `f(x,y) = 43*(1+0.125y)*cos(x/2) + 7.8` 在区间 `x∈[0,10]、y∈[0,10]` 内的最大值。
包含二进制编码/解码、轮盘赌选择、随机单点交叉、多种变异策略（均匀变异、位点变异、逆转变异、互换变异）等核心逻辑。

## 使用方法
### 1. 环境依赖
```bash
pip install numpy
